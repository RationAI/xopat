{
    "id": "empaia-wsi-tile-source",
    // TODO: ensure there is explicitly stated _WHERE_ the module reads data to avoid colision with empaia lib
    "name": "RationAI Enhanced WSI Service by Empaia",
    "version": "0.1.0",
    // Superseded by `rationai-wsi-tile-source`, which matches the same
    // /v3/{files,slides}/info endpoints — load one or the other, never both.
    "stability": "deprecated",
    "includes" : [
        "tile-source.js"
    ],
    "requires": []
}
