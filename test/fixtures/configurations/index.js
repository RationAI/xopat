import {default as controls} from "./controls"
import {default as config} from "./general"
import {default as shaders} from "./shaders"

const withBrowser = {
    viewportWidth: 1024,
    viewportHeight: 700,
};
export {controls, config, shaders, withBrowser}